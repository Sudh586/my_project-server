{
  "name": "json-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
  "start": "./node_modules/.bin/json-server --watch db.json --host 0.0.0.0 --port $PORT"
},

  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
